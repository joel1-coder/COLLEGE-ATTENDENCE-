import React from 'react';

export default function AdminReportReset(){
  return (
    <div style={{ padding: 16 }}>
      <h2>Reset / Reopen Attendance</h2>
      <p>Reset attendance for a date or reopen submissions. (Admin only)</p>
      <p>(Placeholder) Implement select date and reset/reopen actions with confirmation.</p>
    </div>
  );
}
